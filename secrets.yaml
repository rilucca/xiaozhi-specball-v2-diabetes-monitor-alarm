# 1. Wi-Fi Configuration
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# 2. Dexcom Configuration
# Replace with your actual Dexcom username
dexcom_username: "locolucca"

# 3. API & OTA Configuration
# Generate your 32-byte (64-character) key here: https://esphome.io/components/api/
api_encryption_key: "YOUR_GENERATED_KEY_HERE"

# Optional: OTA Password
# ota_password: "YOUR_OTA_PASSWORD_HERE"
